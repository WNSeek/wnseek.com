import { ui, getLangFromUrl } from '../../i18n/ui';

interface Props {
  key: keyof typeof ui.en;
}

const { key } = Astro.props;
const lang = getLangFromUrl(Astro.url);
const text = ui[lang][key] || ui['en'][key] || key;

<Fragment set:html={text} /> 